# API keys used to authenticate requests on each endpoint
#
# When you deploy the service you **must** replace the values
# below with secure random strings.  These keys are compared
# against incoming Bearer tokens.

API_KEY_BARK=change-me-bark
API_KEY_COQUI=change-me-coqui
API_KEY_ELEVENLABS=change-me-elevenlabs
API_KEY_KOKORO=change-me-kokoro

# ElevenLabs configuration
# The official ElevenLabs Python SDK requires an API key which you
# obtain from your ElevenLabs dashboard.  Without a valid key the
# ElevenLabs endpoint will return an error.  You can optionally
# override the default voice and model here.

ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb
ELEVENLABS_MODEL_ID=eleven_flash_v2_5
ELEVENLABS_SAMPLE_RATE=24000

# Kokoro configuration
# Kokoro supports multiple languages via single character
# `lang_code` keys.  See the README for details.

KOKORO_LANG_CODE=a
KOKORO_VOICE=af_heart